const dir = ".scb";
const compiled = `${dir}/compiled`;
const prerendered = `${dir}/prerendered`;
const entryDir = "src";
const entry = `${entryDir}/index.ts`;

export default {
    entry,
    compiled,
    prerendered
}